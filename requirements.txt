# SkillPatch dependencies
# Install with: pip install -r requirements.txt

# Dashboard
streamlit>=1.32.0
plotly>=5.18.0
pandas>=2.0.0

# VLM / NPU inference (Sasha)
numpy>=1.24.0
onnxruntime>=1.17.0
# onnxruntime-directml  -- GPU fallback on Windows, install separately
# For NPU: install AMD Ryzen AI SDK + onnxruntime with QNN EP

# LLM compiler (Vera)
# llama-cpp-python  -- or onnxruntime-directml for Phi-3, install separately

# Robot control (Diya)
# lerobot  -- install per LeRobot docs, install separately

# Dev
pytest>=7.4.0
