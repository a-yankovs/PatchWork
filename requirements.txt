# SkillPatch — Python dependencies
# Install: pip install -r requirements.txt
#
# Hardware-specific packages (AMD Ryzen AI):
#   llama-cpp-python    — requires cmake + C++ compiler; GPU build needs ROCm
#   onnxruntime-directml — AMD GPU (DirectML); Windows only
#   onnxruntime-qnn      — AMD NPU (QNN); requires AMD AI SDK

# --- Core orchestration (Vera) ---
# opencv for webcam capture in orchestrator
opencv-python-headless>=4.8.0
numpy>=1.24.0

# --- LLM compiler backend (Vera — pick one) ---
# Option A: llama-cpp-python (recommended for local GPU inference)
#   Install separately with GPU support:
#   CMAKE_ARGS="-DLLAMA_HIPBLAS=ON" pip install llama-cpp-python   (AMD ROCm)
#   pip install llama-cpp-python                                     (CPU fallback)
llama-cpp-python>=0.2.0

# Option B: ONNX Runtime + HuggingFace tokenizer (DirectML / AMD GPU on Windows)
# Uncomment if using ONNX backend:
# onnxruntime-directml>=1.17.0
# transformers>=4.40.0
# tokenizers>=0.19.0

# --- VLM verifier backend (Sasha) ---
# Install AMD AI SDK first, then the appropriate onnxruntime variant.
# Uncomment one:
# onnxruntime>=1.17.0              # CPU baseline
# onnxruntime-directml>=1.17.0     # AMD GPU fallback

# --- Robot hardware (Diya) ---
# Install from source:
# pip install git+https://github.com/huggingface/lerobot

# --- Trace logger & dashboard (Elias) ---
streamlit>=1.33.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
