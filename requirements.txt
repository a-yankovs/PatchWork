<<<<<<< HEAD
# SkillPatch dependencies
# Install with: pip install -r requirements.txt

# Dashboard
streamlit>=1.32.0
plotly>=5.18.0
pandas>=2.0.0

# VLM / NPU inference (Sasha)
numpy>=1.24.0
onnxruntime>=1.17.0
# onnxruntime-directml  -- GPU fallback on Windows, install separately
# For NPU: install AMD Ryzen AI SDK + onnxruntime with QNN EP

# LLM compiler (Vera)
# llama-cpp-python  -- or onnxruntime-directml for Phi-3, install separately

# Robot control (Diya)
# lerobot  -- install per LeRobot docs, install separately

# Dev
pytest>=7.4.0
=======
# SkillPatch — Python dependencies
# Install: pip install -r requirements.txt --break-system-packages
#
# Hardware-specific packages (AMD ROCm GPU):
#   llama-cpp-python    — requires cmake + C++ compiler; GPU build needs ROCm HIP
#   onnxruntime-rocm    — AMD ROCm GPU (Linux); pip install onnxruntime-rocm
#
# Audio playback (Linux node):
#   sudo apt-get install -y mpg123   (preferred, lowest latency)
#   or: ffmpeg (provides ffplay)

# --- Core orchestration (Vera) ---
# opencv for webcam capture in orchestrator + webcam_stream
opencv-python-headless>=4.8.0
numpy>=1.24.0

# --- Speech-to-text / STT (Vera) ---
# faster-whisper: local on-device transcription (Whisper tiny/base/small/medium)
# sounddevice: cross-platform microphone capture (requires portaudio)
#   Linux:  sudo apt-get install -y portaudio19-dev
#   macOS:  brew install portaudio
faster-whisper>=1.0.0
sounddevice>=0.4.6

# --- LLM compiler backend (Vera — pick one) ---
# Option A: llama-cpp-python (recommended for local GPU inference)
#   Install separately with GPU support:
#   CMAKE_ARGS="-DLLAMA_HIPBLAS=ON" pip install llama-cpp-python   (AMD ROCm)
#   pip install llama-cpp-python                                     (CPU fallback)
llama-cpp-python>=0.2.0

# Option B: ONNX Runtime + HuggingFace tokenizer (ROCm / AMD GPU on Linux)
# Uncomment if using ONNX backend:
# onnxruntime-rocm>=1.17.0
# transformers>=4.40.0
# tokenizers>=0.19.0

# --- VLM verifier backend (Sasha) ---
# Uncomment one:
# onnxruntime>=1.17.0              # CPU baseline
# onnxruntime-rocm>=1.17.0         # AMD ROCm GPU (primary)

# --- Audio feedback / TTS (Vera) ---
# Requires ELEVENLABS_API_KEY env var; silently mocks if not set.
elevenlabs>=1.0.0

# --- Robot hardware (Diya) ---
# Install from source:
# pip install git+https://github.com/huggingface/lerobot

# --- Trace logger & dashboard (Elias) ---
streamlit>=1.33.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
>>>>>>> 56a9020ca3996612163f9887e95843a020e860b6
