// common.cpp — implementations that live outside the header
// Most of common.hpp is inline — this file is intentionally minimal.

#include "tinyvla/common.hpp"
#include <sstream>

namespace tinyvla {

// Placeholder for any non-inline common utilities that grow over time.
// Currently all Vec3 / Rotation3 / Pose methods are header-inline for speed.

} // namespace tinyvla
